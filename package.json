{
  "name": "superjatsi",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "description": "Web-pohjainen maxi-jatsi (Täysi) paikalliseen moninpeliin",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.5"
  }
}
